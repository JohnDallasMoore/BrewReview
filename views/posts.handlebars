{{#each post as |posts| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      <a href="/posts/{{post.id}}">{{post.title}}</a>
    </h2>
    <p>{{post.user.name}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{post.post_text}}
    </p>
  </div>
</div>
{{/each}}
